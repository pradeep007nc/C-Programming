#include<stdio.h>

typedef struct{
    char arr[10];
}stack;

towerOfhanoi(int disks, char source, char temp, char destination);

int main(){
    int n=3;
    char a,b,c;
    printf("%d",towerOfhanoi(n, a, b, c));    
    return 0;
}

void towerOfhanoi(int disks, char source, char temp, char destination){
    int 
}